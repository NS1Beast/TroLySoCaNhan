<ui:FluentWindow x:Class="TroLySoCaNhan.Views.UpgradeDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 mc:Ignorable="d"
                 Title="Cửa hàng dịch vụ"
                 Height="780" Width="880" MinHeight="700" MinWidth="850"
                 WindowBackdropType="Mica" ExtendsContentIntoTitleBar="True"
                 WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
                 TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType">

    <ui:FluentWindow.Resources>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#0F172A"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#64748B"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#EEFFFFFF"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>

        <Style x:Key="PrimaryButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Height" Value="46"/>
            <Setter Property="MinWidth" Value="140"/>
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#1D4ED8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Height" Value="46"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#D8E6FF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#0F172A"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#EFF6FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FFFFFF" Offset="0.00"/>
                <GradientStop Color="#F3F9FF" Offset="0.5"/>
                <GradientStop Color="#DBEAFE" Offset="1.00"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ui:TitleBar Grid.Row="0" Title="Nâng cấp hệ thống" Foreground="#0F172A" Background="Transparent">
                <ui:TitleBar.Icon>
                    <ui:SymbolIcon Symbol="ShoppingBag24" Foreground="#2563EB"/>
                </ui:TitleBar.Icon>
            </ui:TitleBar>

            <Grid Grid.Row="1" Margin="30,10,30,20" Visibility="{Binding ShowSelectPayment, Converter={StaticResource BoolToVisibility}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,35">
                    <TextBlock Text="Nâng Tầm Không Gian Lưu Trữ Của Bạn" FontSize="26" FontWeight="Bold" Foreground="#0F172A" HorizontalAlignment="Center"/>
                    <TextBlock Text="Chọn gói dịch vụ phù hợp để mở khóa sức mạnh AI OCR và lưu trữ E2EE." FontSize="14" Foreground="#64748B" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>

                <ListBox Grid.Row="1" ItemsSource="{Binding AvailablePackages}" SelectedItem="{Binding SelectedPackage, Mode=TwoWay}" Background="Transparent" BorderThickness="0" HorizontalAlignment="Center" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Margin" Value="10,0"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Grid Margin="10,10,10,10">
                                            <Border x:Name="Root" Width="320" Height="360" CornerRadius="24" Padding="24" BorderBrush="Transparent" BorderThickness="4" SnapsToDevicePixels="True" Opacity="0.65">
                                                <Border.Background>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                        <GradientStop Color="{Binding Color1}" Offset="0"/>
                                                        <GradientStop Color="{Binding Color2}" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Border.Background>
                                                <Border.Effect>
                                                    <DropShadowEffect BlurRadius="15" ShadowDepth="5" Opacity="0.1" Color="Black"/>
                                                </Border.Effect>
                                                <ContentPresenter/>
                                            </Border>

                                            <Border x:Name="CheckBadge" Width="44" Height="44" CornerRadius="22" Background="White" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-15,-15,0" Visibility="Hidden" BorderBrush="#10B981" BorderThickness="2">
                                                <Border.Effect>
                                                    <DropShadowEffect BlurRadius="8" ShadowDepth="4" Opacity="0.2"/>
                                                </Border.Effect>
                                                <ui:SymbolIcon Symbol="Checkmark24" Foreground="#10B981" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </Grid>

                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Root" Property="Opacity" Value="0.9"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Root" Property="Opacity" Value="1"/>
                                                <Setter TargetName="Root" Property="BorderBrush" Value="White"/>

                                                <Setter TargetName="Root" Property="Effect">
                                                    <Setter.Value>
                                                        <DropShadowEffect BlurRadius="30" ShadowDepth="12" Opacity="0.4" Color="Black"/>
                                                    </Setter.Value>
                                                </Setter>

                                                <Setter TargetName="CheckBadge" Property="Visibility" Value="Visible"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                    <ui:SymbolIcon Symbol="Star24" Foreground="White" FontSize="26" Margin="0,0,10,0"/>
                                    <TextBlock Text="{Binding TenGoi}" Foreground="White" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
                                </StackPanel>

                                <TextBlock Text="{Binding GiaTienStr}" Foreground="#F8FAFC" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,10"/>
                                <TextBlock Text="{Binding MoTa}" Foreground="#E2E8F0" FontSize="13" TextWrapping="Wrap" LineHeight="20" Margin="0,0,0,20" Height="40"/>

                                <Rectangle Height="1" Fill="#44FFFFFF" Margin="0,0,0,20"/>

                                <ItemsControl ItemsSource="{Binding QuyenLois}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                                <ui:SymbolIcon Symbol="Checkmark24" Foreground="#BBF7D0" FontSize="16" Margin="0,0,10,0"/>
                                                <TextBlock Text="{Binding NoiDung}" Foreground="White" FontWeight="Medium" TextWrapping="Wrap" MaxWidth="250"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel Grid.Row="2" Margin="0,25,0,0" HorizontalAlignment="Center" Width="660">
                    <TextBlock Text="Phương thức thanh toán" FontSize="16" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,10"/>
                    <ListBox ItemsSource="{Binding PhuongThucs}" SelectedItem="{Binding SelectedPhuongThuc, Mode=TwoWay}" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Margin" Value="0,0,15,0"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Grid>
                                                <Border x:Name="Root" Width="210" Background="White" BorderBrush="#D8E6FF" BorderThickness="2" CornerRadius="14" Padding="12">
                                                    <ContentPresenter/>
                                                </Border>
                                                <ui:SymbolIcon x:Name="PaymentCheck" Symbol="CheckmarkCircle24" Foreground="#2563EB" FontSize="20" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,8,8,0" Visibility="Hidden"/>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Root" Property="Background" Value="#EFF6FF"/>
                                                    <Setter TargetName="Root" Property="BorderBrush" Value="#2563EB"/>
                                                    <Setter TargetName="PaymentCheck" Property="Visibility" Value="Visible"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <ui:SymbolIcon Symbol="{Binding Icon.Symbol}" FontSize="24" Foreground="{Binding Color}" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Ten}" Foreground="#0F172A" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>

                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,40,0">
                    <Button Style="{StaticResource SecondaryButtonStyle}" Command="{Binding HuyCommand}" Margin="0,0,15,0">
                        <TextBlock Text="Thoát"/>
                    </Button>
                    <Button Style="{StaticResource PrimaryButtonStyle}" Command="{Binding ThanhToanCommand}">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Payment24" FontSize="18" Margin="0,0,8,0"/>
                            <TextBlock Text="Xác nhận Thanh toán"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Visibility="{Binding ShowWaitingPayment, Converter={StaticResource BoolToVisibility}}">
                <Border Background="White" Width="460" Height="480" CornerRadius="24" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="30" BorderBrush="#E2E8F0" BorderThickness="1">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="30" ShadowDepth="10" Opacity="0.1" Color="Black"/>
                    </Border.Effect>
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="Quét mã để thanh toán" Foreground="#0F172A" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Đang xử lý nâng cấp lên" Foreground="#64748B" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding SelectedPackage.TenGoi}" Foreground="#2563EB" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,5,0,20"/>

                        <Border Width="220" Height="220" CornerRadius="16" BorderBrush="#E2E8F0" BorderThickness="2" Margin="0,0,0,30">
                            <ui:SymbolIcon Symbol="QrCode24" FontSize="160" Foreground="#0F172A" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                            <ui:ProgressRing IsIndeterminate="True" Width="20" Height="20" Foreground="#2563EB"/>
                            <TextBlock Text="Hệ thống đang chờ xác nhận giao dịch..." Foreground="#0F172A" FontWeight="Medium" Margin="10,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>

                        <Button Style="{StaticResource SecondaryButtonStyle}" Command="{Binding HuyCommand}" HorizontalAlignment="Center">
                            <TextBlock Text="Hủy giao dịch"/>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>

            <Grid Grid.Row="1" Visibility="{Binding ShowSuccess, Converter={StaticResource BoolToVisibility}}">
                <Border Background="White" Width="460" Height="360" CornerRadius="24" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="30" BorderBrush="#E2E8F0" BorderThickness="1">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="30" ShadowDepth="10" Opacity="0.1" Color="Black"/>
                    </Border.Effect>
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Border Width="90" Height="90" CornerRadius="45" Background="#10B981" Margin="0,0,0,20">
                            <ui:SymbolIcon Symbol="Checkmark24" FontSize="50" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <TextBlock Text="Giao dịch thành công!" Foreground="#0F172A" FontSize="26" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Tài khoản của bạn đã được cập nhật các quyền lợi mới. Trải nghiệm hệ thống ngay." Foreground="#64748B" TextAlignment="Center" TextWrapping="Wrap" MaxWidth="350" Margin="0,0,0,30"/>

                        <Button Style="{StaticResource PrimaryButtonStyle}" Command="{Binding DongCommand}" HorizontalAlignment="Center">
                            <TextBlock Text="Bắt đầu sử dụng"/>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>

        </Grid>
    </Grid>
</ui:FluentWindow>