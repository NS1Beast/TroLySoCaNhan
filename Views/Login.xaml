<ui:FluentWindow x:Class="TroLySoCaNhan.Views.Login"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:local="clr-namespace:TroLySoCaNhan.Views"
                 mc:Ignorable="d"
                 Title="Đăng nhập - Trợ lý số cá nhân" 
                 Height="650" Width="1000"
                 WindowBackdropType="Mica"
                 ExtendsContentIntoTitleBar="True"
                 WindowStartupLocation="CenterScreen">

    <!-- Thêm event MouseMove để làm hiệu ứng Parallax (nếu bạn có dùng ở code-behind) -->
    <Grid MouseMove="Grid_MouseMove">
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0f172a" Offset="0.0" />
                <GradientStop Color="#1e293b" Offset="1.0" />
            </LinearGradientBrush>
        </Grid.Background>

        <ui:TitleBar Title="Trợ Lý Số Cá Nhân" VerticalAlignment="Top" Foreground="White">
            <ui:TitleBar.Icon>
                <ui:SymbolIcon Symbol="Bot24" Foreground="#3b82f6" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <Grid Margin="0,32,0,0">
            <Grid Width="860" HorizontalAlignment="Center" VerticalAlignment="Center">

                <!-- PHẦN BRANDING (Bên phải) -->
                <StackPanel x:Name="pnlBranding" HorizontalAlignment="Right" VerticalAlignment="Center" Width="380">
                    <StackPanel.RenderTransform>
                        <TranslateTransform x:Name="transBranding" X="0"/>
                    </StackPanel.RenderTransform>
                    <ui:SymbolIcon Symbol="Bot24" FontSize="90" Foreground="#3b82f6" Margin="0,0,0,20"/>
                    <TextBlock Text="Trợ Lý Số Cá Nhân" FontSize="42" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" />
                    <TextBlock Text="Không gian quản lý tài liệu, tài khoản và giao tiếp an toàn, thông minh." FontSize="16" Foreground="#94a3b8" Margin="0,15,0,0" TextWrapping="Wrap" TextAlignment="Center" />
                </StackPanel>

                <!-- PHẦN FORM CARD (Bên trái) -->
                <Border x:Name="cardForm" HorizontalAlignment="Left" VerticalAlignment="Center" Width="410" Height="580" 
                        CornerRadius="16" Background="#1e293b" 
                        BorderBrush="#334155" BorderThickness="1" ClipToBounds="True">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="40" ShadowDepth="0" Opacity="0.5" Color="Black"/>
                    </Border.Effect>
                    <Border.RenderTransform>
                        <TranslateTransform x:Name="transCard" X="0"/>
                    </Border.RenderTransform>

                    <Grid>
                        <!-- 1. PANEL ĐĂNG NHẬP -->
                        <StackPanel x:Name="pnlLogin" Background="Transparent" Margin="40,35">
                            <StackPanel.RenderTransform>
                                <TranslateTransform x:Name="transLogin" X="0"/>
                            </StackPanel.RenderTransform>

                            <TextBlock Text="Chào mừng trở lại!" FontSize="28" FontWeight="Bold" Margin="0,0,0,25" Foreground="White" HorizontalAlignment="Center"/>

                            <TextBlock Text="Tên đăng nhập / Email" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="#94a3b8"/>
                            <ui:TextBox x:Name="txtEmail" PlaceholderText="Nhập email của bạn" Icon="{ui:SymbolIcon Person24}" Margin="0,0,0,15" Height="40" Foreground="White"/>

                            <TextBlock Text="Mật khẩu" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="#94a3b8"/>
                            <ui:PasswordBox x:Name="txtPassword" PlaceholderText="••••••••" Icon="{ui:SymbolIcon Password24}" Margin="0,0,0,10" Height="40" Foreground="White"/>

                            <Grid Margin="0,0,0,20">
                                <CheckBox x:Name="chkRememberMe" Content="Nhớ tài khoản" Foreground="#94a3b8" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <Hyperlink TextDecorations="None" Foreground="#3b82f6" Click="LinkToForgotPassword_Click">Quên mật khẩu?</Hyperlink>
                                </TextBlock>
                            </Grid>

                            <!-- Nút Đăng nhập -->
                            <ui:Button x:Name="btnLogin" Appearance="Primary" Background="#3b82f6" BorderBrush="#3b82f6" HorizontalAlignment="Stretch" Margin="0,0,0,15" Height="45" Click="btnLogin_Click">
                                <Grid>
                                    <StackPanel x:Name="txtLoginBtn" Orientation="Horizontal" HorizontalAlignment="Center">
                                        <ui:SymbolIcon Symbol="ArrowRight24" Margin="0,0,8,0" FontSize="18" Foreground="White"/>
                                        <TextBlock Text="Đăng nhập" FontSize="15" FontWeight="Bold" Foreground="White"/>
                                    </StackPanel>
                                    <ui:ProgressRing x:Name="ringLoading" Visibility="Collapsed" IsIndeterminate="True" Width="20" Height="20" Foreground="White"/>
                                </Grid>
                            </ui:Button>

                            <!-- Đường chia cắt HOẶC -->
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Separator Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0" Background="#334155"/>
                                <TextBlock Grid.Column="1" Text="HOẶC" Foreground="#64748b" FontSize="12"/>
                                <Separator Grid.Column="2" VerticalAlignment="Center" Margin="10,0,0,0" Background="#334155"/>
                            </Grid>

                            <!-- Nút OpenID thay cho tài khoản khách -->
                            <ui:Button x:Name="btnOpenID" Appearance="Secondary" HorizontalAlignment="Stretch" Margin="0,0,0,20" Height="45" Background="#0f172a" BorderBrush="#334155">
                                <StackPanel Orientation="Horizontal">
                                    <ui:SymbolIcon Symbol="ShieldKeyhole24" Foreground="White" Margin="0,0,8,0"/>
                                    <TextBlock Text="Đăng nhập qua OpenID / SSO" Foreground="White" FontWeight="SemiBold"/>
                                </StackPanel>
                            </ui:Button>

                            <!-- Link Đăng ký -->
                            <TextBlock HorizontalAlignment="Center">
                                <Run Text="Chưa có tài khoản? " Foreground="#94a3b8"/>
                                <Hyperlink TextDecorations="None" Foreground="#3b82f6" FontWeight="SemiBold" Click="LinkToRegister_Click">Tạo tài khoản</Hyperlink>
                            </TextBlock>
                        </StackPanel>

                        <!-- 2. PANEL ĐĂNG KÝ (Giữ nguyên cấu trúc ẩn bên phải X=420) -->
                        <StackPanel x:Name="pnlRegister" Background="Transparent" Margin="40,30">
                            <StackPanel.RenderTransform>
                                <TranslateTransform x:Name="transRegister" X="420"/>
                            </StackPanel.RenderTransform>
                            <TextBlock Text="Tạo tài khoản mới" FontSize="26" FontWeight="Bold" Margin="0,0,0,20" Foreground="White" HorizontalAlignment="Center"/>
                            <ui:TextBox x:Name="txtRegName" PlaceholderText="Họ và tên" Icon="{ui:SymbolIcon Person24}" Margin="0,0,0,15" Height="38" Foreground="White"/>
                            <ui:TextBox x:Name="txtRegEmail" PlaceholderText="Email" Icon="{ui:SymbolIcon Mail24}" Margin="0,0,0,15" Height="38" Foreground="White"/>
                            <ui:PasswordBox x:Name="txtRegPassword" PlaceholderText="Mật khẩu" Icon="{ui:SymbolIcon LockClosed24}" Margin="0,0,0,15" Height="38" Foreground="White"/>
                            <ui:PasswordBox x:Name="txtRegConfirmPassword" PlaceholderText="Nhập lại mật khẩu" Icon="{ui:SymbolIcon LockClosed24}" Margin="0,0,0,15" Height="38" Foreground="White"/>

                            <ui:Button x:Name="btnRegister" Appearance="Primary" Background="#3b82f6" BorderBrush="#3b82f6" HorizontalAlignment="Stretch" Margin="0,15,0,20" Height="45" Click="btnRegister_Click">
                                <Grid>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <ui:SymbolIcon Symbol="Add24" Margin="0,0,8,0" FontSize="18" Foreground="White"/>
                                        <TextBlock x:Name="txtRegBtnContent" Text="Đăng ký tài khoản" FontSize="15" FontWeight="Bold" Foreground="White"/>
                                    </StackPanel>
                                    <ui:ProgressRing x:Name="ringRegLoading" Visibility="Collapsed" IsIndeterminate="True" Width="20" Height="20" Foreground="White"/>
                                </Grid>
                            </ui:Button>
                            <TextBlock HorizontalAlignment="Center">
                                <Hyperlink TextDecorations="None" Foreground="#3b82f6" FontWeight="SemiBold" Click="LinkToLogin_Click">Quay lại đăng nhập</Hyperlink>
                            </TextBlock>
                        </StackPanel>

                        <!-- 3. PANEL QUÊN MẬT KHẨU (Giữ nguyên cấu trúc ẩn bên phải X=420) -->
                        <StackPanel x:Name="pnlForgotPassword" Background="Transparent" Margin="40,30">
                            <StackPanel.RenderTransform>
                                <TranslateTransform x:Name="transForgot" X="420"/>
                            </StackPanel.RenderTransform>
                            <TextBlock Text="Khôi phục mật khẩu" FontSize="24" FontWeight="Bold" Margin="0,0,0,15" Foreground="White" HorizontalAlignment="Center"/>
                            <ui:TextBox x:Name="txtForgotEmail" PlaceholderText="Email đăng ký" Icon="{ui:SymbolIcon Mail24}" Margin="0,0,0,15" Height="38" Foreground="White"/>

                            <ui:Button x:Name="btnForgotPassword" Appearance="Primary" Background="#3b82f6" BorderBrush="#3b82f6" HorizontalAlignment="Stretch" Margin="0,15,0,20" Height="45" Click="btnForgotPassword_Click">
                                <Grid>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <ui:SymbolIcon Symbol="Send24" Margin="0,0,8,0" FontSize="18" Foreground="White"/>
                                        <TextBlock x:Name="txtForgotBtnContent" Text="Gửi mã khôi phục" FontSize="15" FontWeight="Bold" Foreground="White"/>
                                    </StackPanel>
                                    <ui:ProgressRing x:Name="ringForgotLoading" Visibility="Collapsed" IsIndeterminate="True" Width="20" Height="20" Foreground="White"/>
                                </Grid>
                            </ui:Button>
                            <TextBlock HorizontalAlignment="Center">
                                <Hyperlink TextDecorations="None" Foreground="#3b82f6" FontWeight="SemiBold" Click="LinkToLoginFromForgot_Click">Quay lại</Hyperlink>
                            </TextBlock>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</ui:FluentWindow>